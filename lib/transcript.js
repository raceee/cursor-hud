"use strict";

const { createTranscript, applyHudEvent, mergeAssistantText, formatAssistantText } = require("../renderer/core");

module.exports = {
  createTranscript,
  applyHudEvent,
  mergeAssistantText,
  formatAssistantText,
};
